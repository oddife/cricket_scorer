# Docker build-time configuration for Flutter Web.
# Copy to .env and fill in the values before running docker compose build.
# The Supabase publishable key is intended for client-side use.

SUPABASE_URL=https://supabase.example.com/
SUPABASE_PUBLISHABLE_KEY=your-publishable-key
